# Налаштування SSL/TLS сертифіката та домену для застосунку

Цей посібник допоможе вам налаштувати безкоштовний домен, згенерувати SSL/TLS сертифікат через AWS Certificate Manager (ACM) та налаштувати DNS для маршрутизації HTTPS трафіку.

## 📋 Передумови

- AWS акаунт з налаштованими credentials
- Terraform встановлено (версія >= 1.0)
- Базова інфраструктура розгорнута (VPC, ALB, EC2)

## 🌐 Крок 1: Реєстрація безкоштовного домену

### Варіанти безкоштовних доменів з підтримкою ACME:

#### Опція 1: Freenom (Рекомендовано для початківців)
1. Перейдіть на [freenom.com](https://www.freenom.com)
2. Зареєструйтесь та оберіть безкоштовний домен (.tk, .ml, .ga, .cf, .gq)
3. Наприклад: `yourapp.tk`
4. Термін дії: безкоштовно до 12 місяців з можливістю продовження

#### Опція 2: DuckDNS
1. Перейдіть на [duckdns.org](https://www.duckdns.org)
2. Авторизуйтесь через GitHub/Google/Reddit
3. Створіть subdomain: `yourapp.duckdns.org`
4. Безстроково безкоштовно

#### Опція 3: eu.org
1. Перейдіть на [eu.org](https://nic.eu.org)
2. Зареєструйтесь та подайте заявку на subdomain
3. Наприклад: `yourapp.eu.org`
4. Розгляд заявки: 1-3 дні

#### Опція 4: afraid.org
1. Перейдіть на [afraid.org](https://freedns.afraid.org)
2. Створіть акаунт
3. Оберіть безкоштовний домен зі списку
4. Безстроково безкоштовно

## 🔐 Крок 2: Налаштування Terraform змінних

Відкрийте `terraform.tfvars` і додайте/оновіть наступні змінні:

```hcl
# Увімкнути HTTPS
enable_https = true

# Ваш зареєстрований домен
domain_name = "yourapp.tk"  # Замініть на ваш домен

# Створювати Route53 zone (якщо використовуєте Freenom/eu.org)
create_route53_zone = true

# Додаткові домени для сертифіката (опціонально)
subject_alternative_names = ["www.yourapp.tk"]

# Додайте порт 443 для HTTPS
alb_ingress_ports = [80, 443]
```

### Якщо використовуєте існуючу DNS зону:
```hcl
create_route53_zone = false
route53_zone_id     = "Z0123456789ABCDEFGHIJ"  # Ваш існуючий Zone ID
```

## 🚀 Крок 3: Розгортання інфраструктури з SSL

### 3.1 Ініціалізація Terraform
```powershell
cd terraform
terraform init
```

### 3.2 Перегляд змін
```powershell
terraform plan
```

Ви побачите ресурси, які будуть створені:
- Route53 Hosted Zone
- ACM Certificate
- DNS validation records
- HTTPS listener на ALB
- HTTP → HTTPS redirect

### 3.3 Застосування змін
```powershell
terraform apply
```

Підтвердіть створення ресурсів вводом `yes`.

⏱️ **Примітка**: Процес валідації сертифіката може зайняти 5-30 хвилин.

### 3.4 Отримання важливих output значень
```powershell
terraform output
```

Збережіть наступну інформацію:
- `route53_name_servers` - Name Servers для конфігурації в реєстраторі
- `certificate_arn` - ARN сертифіката
- `application_url` - URL вашого застосунку з HTTPS

## 🔧 Крок 4: Налаштування Name Servers у реєстратора домену

### Для Freenom:
1. Увійдіть в [Freenom Client Area](https://my.freenom.com/clientarea.php)
2. Перейдіть до **Services → My Domains**
3. Натисніть **Manage Domain** для вашого домену
4. Перейдіть на вкладку **Management Tools → Nameservers**
5. Оберіть **Use custom nameservers**
6. Введіть 4 Name Servers з terraform output:
   ```
   ns-123.awsdns-12.com
   ns-456.awsdns-45.net
   ns-789.awsdns-78.org
   ns-012.awsdns-01.co.uk
   ```
7. Натисніть **Change Nameservers**

### Для DuckDNS:
1. Увійдіть на [duckdns.org](https://www.duckdns.org)
2. У вашому subdomain натисніть **update ip**
3. Встановіть IP на DNS name вашого ALB (з `terraform output alb_dns_name`)
4. Або створіть CNAME запис вручну, якщо підтримується

### Для eu.org:
1. Увійдіть в панель керування eu.org
2. Перейдіть до налаштувань домену
3. Встановіть Name Servers з terraform output
4. Збережіть зміни

### Для afraid.org:
1. Увійдіть на afraid.org
2. Перейдіть до **Subdomains**
3. Редагуйте ваш subdomain
4. Встановіть Type: **A** та Destination: ALB DNS name
5. Або встановіть Custom Name Servers

⏱️ **Час propagation**: DNS зміни можуть зайняти від 5 хвилин до 48 годин для повної propagation по всьому світу.

## ✅ Крок 5: Перевірка SSL сертифіката

### 5.1 Перевірка статусу сертифіката в AWS Console
1. Відкрийте [AWS Certificate Manager Console](https://console.aws.amazon.com/acm)
2. Оберіть регіон, де розгорнуто інфраструктуру
3. Знайдіть ваш сертифікат
4. Переконайтесь, що статус: **Issued** ✅

### 5.2 Перевірка DNS propagation
```powershell
# Windows
nslookup yourapp.tk

# Або використайте онлайн інструменти:
# https://dnschecker.org
# https://www.whatsmydns.net
```

### 5.3 Тестування HTTPS з'єднання

#### В браузері:
1. Відкрийте `https://yourapp.tk`
2. Перевірте наявність замка 🔒 в адресному рядку
3. Клікніть на замок → переглянути сертифікат
4. Переконайтесь, що сертифікат виданий для вашого домену

#### З командного рядка:
```powershell
# Перевірка SSL сертифіката
curl -v https://yourapp.tk

# Перевірка редіректу HTTP → HTTPS
curl -I http://yourapp.tk
# Повинен повернути 301 Moved Permanently → https://yourapp.tk
```

### 5.4 Перевірка через SSL Labs
1. Відкрийте [SSL Labs Server Test](https://www.ssllabs.com/ssltest/)
2. Введіть ваш домен: `yourapp.tk`
3. Дочекайтесь завершення тесту
4. Переконайтесь в рейтингу **A** або вище

## 🔍 Troubleshooting

### Проблема: Сертифікат залишається в статусі "Pending validation"

**Рішення:**
1. Перевірте, чи DNS records для валідації створені в Route53:
   ```powershell
   aws route53 list-resource-record-sets --hosted-zone-id Z0123... --query "ResourceRecordSets[?Type=='CNAME']"
   ```

2. Перевірте, чи Name Servers правильно налаштовані у реєстратора

3. Дочекайтесь DNS propagation (до 48 годин)

### Проблема: Домен не резолвиться

**Рішення:**
1. Перевірте Name Servers:
   ```powershell
   nslookup -type=NS yourapp.tk
   ```
   
2. Переконайтесь, що Name Servers в реєстраторі співпадають з Route53 output

3. Почекайте повної DNS propagation

### Проблема: HTTPS не працює, але HTTP працює

**Рішення:**
1. Перевірте, чи порт 443 відкритий в Security Group ALB
2. Переконайтесь, що HTTPS listener створено:
   ```powershell
   terraform output
   ```
3. Перевірте статус сертифіката в ACM Console

### Проблема: Certificate validation timeout

**Рішення:**
1. Вручну створіть CNAME запис для валідації:
   - Name: отримайте з ACM Console
   - Value: отримайте з ACM Console
   - TTL: 300

2. Або використайте email validation замість DNS (потрібно змінити код)

## 📊 Архітектура HTTPS Traffic Flow

```
[User Browser]
    ↓ HTTPS (443)
[Domain: yourapp.tk] → [Route53 DNS]
    ↓ resolves to
[Application Load Balancer]
    ↓ SSL/TLS termination (certificate from ACM)
    ↓ HTTP (80/8080 internal)
[EC2 Instances: React / Angular / Backend]
```

## 🔄 Оновлення та підтримка

### Renewal сертифіката
ACM автоматично оновлює сертифікати **за 60 днів до закінчення терміну дії**. Ніяких дій не потрібно!

### Моніторинг expiration
```powershell
aws acm describe-certificate --certificate-arn arn:aws:acm:region:account:certificate/xxx
```

### Додавання нових доменів
1. Оновіть `subject_alternative_names` в `terraform.tfvars`
2. Запустіть `terraform apply`
3. Сертифікат буде перевиданий з новими доменами

## 📚 Додаткові ресурси

- [AWS Certificate Manager Documentation](https://docs.aws.amazon.com/acm/)
- [Route53 Documentation](https://docs.aws.amazon.com/route53/)
- [Freenom Documentation](https://www.freenom.com/en/support.html)
- [Let's Encrypt ACME Protocol](https://letsencrypt.org/docs/challenge-types/)

## 🎯 Успішне завершення

Після виконання всіх кроків ви матимете:
- ✅ Зареєстрований безкоштовний домен
- ✅ Дійсний SSL/TLS сертифікат від AWS ACM
- ✅ Route53 DNS зону з правильними записами
- ✅ HTTPS трафік маршрутизується до вашого застосунку
- ✅ Автоматичний редірект з HTTP на HTTPS
- ✅ A+ рейтинг SSL Labs

Ваш застосунок тепер доступний за адресою: **https://yourapp.tk** 🎉
