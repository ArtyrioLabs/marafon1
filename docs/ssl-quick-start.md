# Quick Start: SSL/TLS Setup (5-хвилинний гайд)

Швидкий старт для налаштування HTTPS для вашого застосунку.

## 🚀 Швидкі кроки

### 1. Зареєструйте безкоштовний домен (2 хвилини)

**Рекомендація: Freenom** (найпростіший варіант)

1. Перейдіть на https://www.freenom.com
2. Введіть бажану назву домену → **Check Availability**
3. Оберіть безкоштовний TLD (.tk, .ml, .ga, .cf, .gq)
4. **Checkout** → зареєструйтесь → **Complete Order**
5. Збережіть ваш домен, наприклад: `myapp.tk`

### 2. Оновіть Terraform конфігурацію (1 хвилина)

Відкрийте `terraform/terraform.tfvars`:

```hcl
# Змініть ці два рядки:
enable_https = true
domain_name = "myapp.tk"  # Ваш домен з Freenom

# Переконайтесь, що 443 є в списку:
alb_ingress_ports = [80, 443, 8080]
```

### 3. Застосуйте Terraform (5-10 хвилин)

```powershell
cd terraform
terraform apply
```

Введіть `yes` для підтвердження.

⏱️ Terraform створить:
- Route53 DNS зону
- SSL сертифікат в ACM
- HTTPS listener на ALB
- DNS records

**Збережіть output з Name Servers!**

### 4. Налаштуйте Name Servers в Freenom (2 хвилини)

1. Відкрийте https://my.freenom.com/clientarea.php
2. **Services → My Domains → Manage Domain**
3. **Management Tools → Nameservers**
4. Оберіть **Use custom nameservers**
5. Скопіюйте 4 Name Servers з terraform output:
   ```
   terraform output route53_name_servers
   ```
6. Вставте їх в Freenom і натисніть **Change Nameservers**

### 5. Дочекайтесь (5-30 хвилин)

☕ Зробіть каву, поки:
- DNS propagation завершиться (5-15 хв)
- ACM провалідує сертифікат (10-30 хв)

Перевірити статус:
```powershell
# Перевірка DNS
nslookup myapp.tk

# Перевірка сертифіката в AWS Console
# https://console.aws.amazon.com/acm
```

### 6. Перевірте HTTPS ✅

Відкрийте в браузері:
```
https://myapp.tk
```

Переконайтесь в наявності 🔒 замка!

---

## 🔧 Альтернатива: DuckDNS (ще простіше, але subdomain)

### 1. Реєстрація (1 хвилина)
1. Відкрийте https://www.duckdns.org
2. Авторизуйтесь через GitHub/Google
3. Створіть subdomain: `myapp` → отримаєте `myapp.duckdns.org`

### 2. Налаштування (відрізняється від Freenom)

В `terraform.tfvars`:
```hcl
enable_https = true
domain_name = "myapp.duckdns.org"
create_route53_zone = false  # DuckDNS керує DNS сам
```

### 3. В DuckDNS панелі
1. В полі **current ip** вставте DNS name вашого ALB:
   ```powershell
   terraform output alb_dns_name
   ```
2. Натисніть **update ip**

**Примітка**: З DuckDNS вам потрібно використовувати Let's Encrypt напряму (не через ACM), або налаштувати CNAME на ALB. Для початківців рекомендую Freenom з Route53.

---

## ❓ Проблеми?

### Сертифікат не валідується
```powershell
# Перевірте DNS records
aws route53 list-resource-record-sets --hosted-zone-id $(terraform output -raw route53_zone_id)

# Перевірте Name Servers
nslookup -type=NS myapp.tk
```

### Домен не відкривається
1. Перевірте, чи DNS propagation завершено: https://dnschecker.org
2. Дочекайтесь до 48 годин (зазвичай 5-15 хвилин)

### HTTPS не працює
1. Перевірте статус сертифіката в AWS ACM Console
2. Переконайтесь, що порт 443 відкритий в Security Group

---

## 📋 Checklist

- [ ] Домен зареєстровано
- [ ] `enable_https = true` в terraform.tfvars
- [ ] `domain_name` встановлено
- [ ] `terraform apply` виконано успішно
- [ ] Name Servers скопійовано з output
- [ ] Name Servers встановлено в реєстраторі
- [ ] DNS propagation завершено (nslookup)
- [ ] Сертифікат має статус "Issued" в ACM
- [ ] Сайт відкривається через HTTPS з 🔒

---

**Детальні інструкції**: `docs/ssl-setup-guide.md`
