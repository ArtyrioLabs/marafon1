# Налаштування SSL через Cloudflare для Secret Nick

## Передумови
- Домен: `secret-nick.duckdns.org` (або інший безкоштовний домен)
- AWS ALB DNS: `app-alb-1868129411.eu-central-1.elb.amazonaws.com`
- AWS ALB IP: `3.124.207.23` і `18.184.75.36`

---

## 📋 Покрокова інструкція

### Крок 1: Реєстрація на Cloudflare

1. Йдіть на https://dash.cloudflare.com/sign-up
2. Заповніть форму реєстрації:
   - Email
   - Пароль
3. Підтвердіть email

### Крок 2: Додати сайт в Cloudflare

1. Після входу натисніть **"Add a site"**
2. Введіть ваш домен:
   - Для DuckDNS: `secret-nick.duckdns.org`
   - Для Freenom домену: `your-domain.tk` (або .ml, .ga, .cf, .gq)
3. Виберіть план: **Free** (безкоштовний)
4. Натисніть **"Continue"**

### Крок 3: Налаштувати DNS записи в Cloudflare

Cloudflare автоматично спробує імпортувати існуючі DNS записи. Переконайтесь, що є наступні записи:

#### A запис (основний домен)
| Type | Name | Content | Proxy status | TTL |
|------|------|---------|--------------|-----|
| A | @ | 3.124.207.23 | ✅ Proxied | Auto |

#### Якщо потрібно www піддомен:
| Type | Name | Content | Proxy status | TTL |
|------|------|---------|--------------|-----|
| CNAME | www | secret-nick.duckdns.org | ✅ Proxied | Auto |

**⚠️ ВАЖЛИВО:** 
- **Proxy status** повинен бути **Proxied** (оранжева хмарка) для SSL
- Це означає, що Cloudflare буде проксувати весь трафік і надавати SSL

### Крок 4: Змінити Nameservers (тільки якщо НЕ DuckDNS)

**Якщо ви використовуєте DuckDNS:**
- ⚠️ **DuckDNS не підтримує зміну nameservers**
- Пропустіть цей крок
- Використайте CNAME setup (дивіться Крок 5)

**Якщо ви використовуєте Freenom або інший провайдер:**
1. Cloudflare покаже вам 2 nameservers, наприклад:
   ```
   aria.ns.cloudflare.com
   sergio.ns.cloudflare.com
   ```
2. Йдіть в панель управління вашого домен провайдера
3. Знайдіть налаштування DNS/Nameservers
4. Замініть існуючі nameservers на nameservers Cloudflare
5. Збережіть зміни
6. Зачекайте 24-48 годин на оновлення (зазвичай 1-2 години)

### Крок 5: CNAME Setup для DuckDNS (альтернатива)

Якщо ви використовуєте DuckDNS і не можете змінити nameservers:

1. **В Cloudflare:**
   - Створіть піддомен, наприклад: `app.yourdomain.com`
   - Додайте CNAME запис:
     ```
     Type: CNAME
     Name: app
     Target: secret-nick.duckdns.org
     Proxy: Proxied (оранжева хмарка)
     ```

2. **В DuckDNS:**
   - Залиште `secret-nick.duckdns.org` з IP `3.124.207.23`

3. **Використовуйте Cloudflare домен:**
   - Замість `secret-nick.duckdns.org` використовуйте `app.yourdomain.com`

### Крок 6: Увімкнути SSL в Cloudflare

1. В Cloudflare dashboard перейдіть в **SSL/TLS**
2. Виберіть режим шифрування:
   - Виберіть **"Full"** або **"Full (strict)"**
   - **Full**: Cloudflare ↔️ Origin (ваш сервер) використовує SSL, але сертифікат може бути самопідписаним
   - **Full (strict)**: Вимагає валідний SSL сертифікат на вашому сервері

3. Рекомендую: **Full**

### Крок 7: Увімкнути Always Use HTTPS

1. В Cloudflare dashboard перейдіть в **SSL/TLS** → **Edge Certificates**
2. Увімкніть **"Always Use HTTPS"**
3. Це автоматично перенаправлятиме HTTP → HTTPS

### Крок 8: Налаштувати Origin Certificate (для Full strict)

Якщо ви вибрали "Full (strict)", потрібно згенерувати Origin Certificate:

1. В Cloudflare: **SSL/TLS** → **Origin Server**
2. Натисніть **"Create Certificate"**
3. Виберіть:
   - Private key type: RSA
   - Domains: `secret-nick.duckdns.org`, `*.secret-nick.duckdns.org`
   - Validity: 15 years
4. Натисніть **"Create"**
5. Збережіть:
   - **Origin Certificate** (cert.pem)
   - **Private Key** (key.pem)

6. Завантажте ці сертифікати на ваш EC2 інстанс або імпортуйте в ACM

### Крок 9: Перевірити SSL

Після того, як DNS оновиться (1-2 години):

1. Відкрийте в браузері: `https://secret-nick.duckdns.org`
2. Перевірте, що:
   - ✅ З'являється замок в адресній строці
   - ✅ Сертифікат виданий Cloudflare
   - ✅ Сайт працює через HTTPS

### Крок 10: Перевірити SSL рейтинг

Перевірте якість SSL конфігурації:
1. Йдіть на https://www.ssllabs.com/ssltest/
2. Введіть ваш домен: `secret-nick.duckdns.org`
3. Натисніть **"Submit"**
4. Зачекайте 2-3 хвилини
5. Ви повинні отримати рейтинг **A** або **A+**

---

## 🎯 Альтернативний варіант: Використати безкоштовний домен через Freenom

### Крок 1: Зареєструвати домен на Freenom

1. Йдіть на https://www.freenom.com/
2. Введіть бажане ім'я домену (наприклад: `secretnick`)
3. Виберіть одну з безкоштовних зон:
   - `.tk` (Токелау)
   - `.ml` (Малі)
   - `.ga` (Габон)
   - `.cf` (Центральноафриканська Республіка)
   - `.gq` (Екваторіальна Гвінея)
4. Натисніть **"Get it now!"**
5. Виберіть період: **12 Months @ FREE**
6. Натисніть **"Continue"**
7. Зареєструйтесь і завершіть замовлення

### Крок 2: Перенести домен на Cloudflare

1. Після реєстрації домену на Freenom, йдіть в Cloudflare
2. Додайте сайт (як описано вище)
3. Cloudflare покаже nameservers
4. В Freenom панелі:
   - **Services** → **My Domains**
   - Натисніть **"Manage Domain"**
   - Виберіть **"Management Tools"** → **"Nameservers"**
   - Виберіть **"Use custom nameservers"**
   - Введіть 2 nameservers з Cloudflare
   - Натисніть **"Change Nameservers"**

5. Зачекайте 1-2 години на оновлення DNS

### Крок 3: Налаштувати DNS в Cloudflare

Після того, як nameservers оновляться:

1. В Cloudflare додайте A запис:
   ```
   Type: A
   Name: @
   Content: 3.124.207.23
   Proxy: Proxied
   ```

2. Додайте CNAME для www (опціонально):
   ```
   Type: CNAME
   Name: www
   Target: your-domain.tk
   Proxy: Proxied
   ```

### Крок 4: Оновити terraform.tfvars

Оновіть ваш `terraform/terraform.tfvars`:

```hcl
domain_name         = "secretnick.tk"  # Ваш новий домен
enable_https        = true
create_route53_zone = false  # Використовуємо Cloudflare замість Route53
```

### Крок 5: Оновити DuckDNS (опціонально)

Якщо ви хочете залишити DuckDNS як backup:

1. В DuckDNS залиште `secret-nick.duckdns.org` з IP `3.124.207.23`
2. В Cloudflare додайте CNAME:
   ```
   Type: CNAME
   Name: backup
   Target: secret-nick.duckdns.org
   Proxy: Proxied
   ```

Тепер ви матимете доступ через:
- `https://secretnick.tk` (через Cloudflare SSL)
- `http://secret-nick.duckdns.org` (через DuckDNS без SSL)

---

## 🔧 Troubleshooting

### SSL не працює після налаштування

1. **Перевірте DNS:**
   ```bash
   nslookup secret-nick.duckdns.org
   ```
   Повинен повертати IP Cloudflare (не ваш ALB IP)

2. **Перевірте SSL режим:**
   - Переконайтесь, що в Cloudflare вибрано **Full** або **Full (strict)**

3. **Перевірте Proxy status:**
   - DNS записи повинні мати статус **Proxied** (оранжева хмарка)

4. **Очистіть кеш браузера:**
   - Натисніть Ctrl+Shift+Delete
   - Очистіть кеш і cookies

5. **Перевірте через інший браузер або incognito mode**

### Cloudflare показує помилку 521 (Web server is down)

1. Перевірте, що ваш ALB працює:
   ```bash
   curl -I http://app-alb-1868129411.eu-central-1.elb.amazonaws.com
   ```

2. Перевірте Security Group:
   - Переконайтесь, що порти 80 і 443 відкриті
   - Додайте Cloudflare IP ranges: https://www.cloudflare.com/ips/

### Cloudflare показує помилку 525 (SSL handshake failed)

1. Змініть SSL режим на **Full** (не strict)
2. Або згенеруйте Origin Certificate і встановіть на сервер

---

## 📚 Додаткові ресурси

- [Cloudflare SSL Modes](https://developers.cloudflare.com/ssl/origin-configuration/ssl-modes/)
- [Cloudflare Origin CA certificates](https://developers.cloudflare.com/ssl/origin-configuration/origin-ca/)
- [Freenom Documentation](https://www.freenom.com/en/support.html)
- [DuckDNS FAQ](https://www.duckdns.org/faqs.jsp)

---

## ✅ Чеклист

- [ ] Зареєструвався на Cloudflare
- [ ] Додав сайт в Cloudflare
- [ ] Налаштував DNS A запис з Proxied
- [ ] Змінив nameservers (якщо не DuckDNS)
- [ ] Увімкнув SSL режим "Full"
- [ ] Увімкнув "Always Use HTTPS"
- [ ] Перевірив https:// доступ
- [ ] Перевірив SSL рейтинг на ssllabs.com
- [ ] Оновив terraform.tfvars з новим доменом

---

**🎉 Готово! Тепер ваш Secret Nick працює з безкоштовним SSL від Cloudflare!**
