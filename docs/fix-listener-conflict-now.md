# ⚡ Срочное исправление: Конфликт listener на порту 80

## Текущая ситуация

Terraform создал listener на порту 80 с типом "forward", но затем пытается создать redirect listener на том же порту → конфликт!

## ✅ Решение: Удалить созданный listener

### Шаг 1: Удалить listener на порту 80 (forward)

1. **AWS Console** → **EC2** → **Load Balancers**
2. Найдите **`app-alb`** и откройте его
3. Перейдите на вкладку **Listeners**
4. Найдите listener на **порту 80** с типом **"forward"** (не "redirect")
5. Выделите его (чекбокс)
6. **Actions** → **Delete** → подтвердите

**Важно:** Не удаляйте listener на порту 443 (HTTPS)!

### Шаг 2: Запустить terraform apply снова

После удаления listener запустите `terraform apply` снова (через GitHub Actions или локально).

Terraform создаст:
- ✅ HTTP redirect listener на порту 80 (HTTP → HTTPS)
- ✅ HTTPS listener на порту 443 (уже создан)
- ✅ Route53 A-запись (уже создана)

---

## 🔍 Как отличить listener

В списке listeners:
- **Port 80, Type: forward** → ❌ Удалить (старый)
- **Port 80, Type: redirect** → ✅ Оставить (новый, будет создан)
- **Port 443, Type: forward** → ✅ Оставить (HTTPS)

---

## 📝 После исправления

1. **Проверьте listeners:**
   - Должен быть только redirect listener на порту 80
   - Должен быть HTTPS listener на порту 443

2. **Проверьте сайт:**
   - Откройте `https://sekret-nick.pp.ua`
   - Должен появиться замок 🔒

---

## 🎯 Итог

1. ✅ Удалить forward listener на порту 80
2. ✅ Запустить terraform apply
3. ✅ Проверить сайт

После этого все должно работать! 🎉

